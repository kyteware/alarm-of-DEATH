# Discord Photo Bot

## Setup
npm run register  # Run once to register commands
npm start

### Send Image via Script
python send_image.py
This sends an image immediately to the channel defined in `CHANNEL_ID`.
